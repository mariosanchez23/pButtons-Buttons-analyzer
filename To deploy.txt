To deploy

1) Copy files changed to installPackage
2) 
Export current package and add it to classes.xml in the pButtonsWeb directory

Do $System.OBJ.ExportPackage("pButtons","/tmp/classes.xml")
!mv /tmp/classes.xml /Users/msanchez/isc/Atelier/workspace/Power\ pButtons/installPackage/pButtonsWeb/classes.xml


Then zip the folder installPackage
